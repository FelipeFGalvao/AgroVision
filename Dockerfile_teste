# ==============================================================================
# Dockerfile Mínimo para Depuração
# Objetivo: Isolar o problema do WebSocket com uma aplicação simples.
# ==============================================================================

# Usa a mesma imagem base de Python para consistência.
FROM python:3.10-slim

# Define o diretório de trabalho dentro do contêiner.
WORKDIR /app

# Instala apenas a dependência essencial para este teste.
# Não usamos o requirements.txt para manter o teste isolado.
RUN pip install streamlit

# Copia apenas o ficheiro da aplicação de teste.
COPY app_test.py .

# Expõe a porta padrão do Streamlit.
EXPOSE 8501

# Define o ponto de entrada para executar a aplicação de teste.
# Usamos apenas as flags essenciais para este teste inicial.
CMD ["streamlit", "run", "app_teste.py", "--server.port=8501", "--server.address=0.0.0.0", "--server.headless=true"]

